import { Box } from '@mui/material';
import styled from 'styled-components';

export const HomeContainer = styled.div`
  margin-top: 100px;
  text-align: center;
`;

export const HomeButtonsContainer = styled(Box)`
  display: flex;
  justify-content: space-between;
  max-width: 400px;
  margin: 0 auto;
`;
